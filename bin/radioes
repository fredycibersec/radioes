#!/usr/bin/env bash
# RadioES launcher — resolves paths regardless of cwd or symlinks
SCRIPT="$(readlink -f "$0")"
BIN_DIR="$(dirname "$SCRIPT")"
APP_DIR="$(dirname "$BIN_DIR")"
exec python3 "$APP_DIR/main.py" "$@"
